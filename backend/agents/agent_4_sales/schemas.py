"""Pydantic output schema for Agent 4 (Sales) — fields defined when agent is implemented."""

from pydantic import BaseModel


class Agent4Output(BaseModel):
    """Pydantic output schema for Agent 4 (Sales) — fields defined when agent is implemented."""

    pass

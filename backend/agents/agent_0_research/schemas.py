"""Pydantic output schema for Agent 0 (Research) — fields defined when agent is implemented."""

from pydantic import BaseModel


class Agent0Output(BaseModel):
    """Pydantic output schema for Agent 0 (Research) — fields defined when agent is implemented."""

    pass
